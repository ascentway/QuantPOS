spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379

stripe:
  api-key: sk_test_mock
  webhook-secret: whsec_mock

langchain4j:
  open-ai:
    chat-model:
      api-key: mock-key
      model-name: gpt-4o-mini

app:
  jwt:
    secret: thisisverysupersecretjwtkeyforquantposanditmustbe256bitslong
    access-expiry-ms: 900000
    refresh-expiry-days: 7
