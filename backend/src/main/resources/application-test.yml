spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/quantpos_test
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379

stripe:
  api-key: sk_test_mock
  webhook-secret: whsec_mock

langchain4j:
  open-ai:
    chat-model:
      api-key: mock-key
      model-name: gpt-4o-mini
